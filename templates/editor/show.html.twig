{% extends 'back_layout.html.twig' %}

{% block title %}Détail de l'éditeur{% endblock %}

{% block content %}
    <h1>Détail de l'éditeur "{{ editor.name }}"</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ editor.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ editor.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_editor_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('app_editor_edit', {'id': editor.id}) }}" class="btn btn-light">edit</a>

    {{ include('editor/_delete_form.html.twig') }}
{% endblock %}