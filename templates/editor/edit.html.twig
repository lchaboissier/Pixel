{% extends 'back_layout.html.twig' %}

{% block title %}Modification de l'éditeur{% endblock %}

{% block content %}
    <h1>Modification de l'éditeur "{{ editor.name }}"</h1>
    
    {{ include('editor/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_editor_index') }}" class="btn btn-primary">Annuler</a>

    {{ include('editor/_delete_form.html.twig') }}
{% endblock %}